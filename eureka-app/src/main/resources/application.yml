spring:
  application:
    name: eureka

eureka:
  instance:
    hostname: eureka
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
management:
  security:
    enabled: false
